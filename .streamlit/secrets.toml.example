# Đây chỉ là file MẪU — không chứa token thật.
# KHÔNG tạo file "secrets.toml" (không có .example) trong repo và commit lên git
# (đã có trong .gitignore).
#
# Cách dùng:
# - Chạy LOCAL (streamlit run app.py): không cần file này, app đọc từ .env.
# - Deploy Streamlit Community Cloud: vào trang quản lý app → Settings → Secrets
#   → dán đúng 2 dòng bên dưới (thay giá trị thật vào), KHÔNG dán file này lên git.

ADJUST_API_TOKEN = "dan_token_that_vao_day"
ADJUST_APP_TOKENS = "app_token_1,app_token_2"
